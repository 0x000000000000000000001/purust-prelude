pub fn Data_Semiring_intAdd(mut a: i64, mut b: i64) -> i64 {
    a + b
}

pub fn Data_Semiring_intMul(mut a: i64, mut b: i64) -> i64 {
    a * b
}

pub fn Data_Semiring_numAdd(mut a: f64, mut b: f64) -> f64 {
    a + b
}

pub fn Data_Semiring_numMul(mut a: f64, mut b: f64) -> f64 {
    a * b
}
